---
title: Canvas Button
date: 2024-03-19
description: An artistic button component with paint-inspired styles and effects.
author: ui-canvas
published: true
---

<ComponentPreview name="canvas-button-demo" />

## Installation

<Steps>

<Step>Install the following dependencies in your project:</Step>

```bash
npm install @radix-ui/react-slot
```

<Step>Copy and paste the following code into your project.</Step>

`components/ui-canvas/canvas-button.tsx`

<ComponentSource name="canvas-button" />

<Step>Update the import paths to match your project setup.</Step>

```bash
import { CanvasButton } from "@/components/ui-canvas/canvas-button";
```

</Steps>

### Example Implementation

```bash
import { CanvasButton } from "@/components/ui-canvas/canvas-button";

// Portfolio contact button
export function ContactSection() {
  return (
    <div className="space-y-4">
      <h3>Get in Touch</h3>
      <CanvasButton variant="watercolor" onClick={() => openContactForm()}>
        Start a Project
      </CanvasButton>
    </div>
  );
}

// Art gallery navigation
export function GalleryNav() {
  return (
    <div className="flex gap-4">
      <CanvasButton variant="oil">Previous Artwork</CanvasButton>
      <CanvasButton variant="oil">Next Artwork</CanvasButton>
    </div>
  );
}
```

## Props

### CanvasButton

| Prop name | Type                                             | Default   | Description                 |
| --------- | ------------------------------------------------ | --------- | --------------------------- |
| variant   | "default" \| "watercolor" \| "oil" \| "charcoal" | "default" | The button's artistic style |
| size      | "sm" \| "default" \| "lg"                        | "default" | The button's size           |
| effect    | "none" \| "splash" \| "brush"                    | "none"    | The interactive effect      |
| asChild   | boolean                                          | false     | Merge with child element    |
| className | string                                           | -         | Additional CSS classes      |

## Examples

### Basic Button

```bash
import { CanvasButton } from "@/components/ui-canvas/canvas-button";

export default function Example() {
  return <CanvasButton>Click me</CanvasButton>;
}
```

### Artistic Styles

```bash
<CanvasButton variant="watercolor">Watercolor</CanvasButton>
<CanvasButton variant="oil">Oil Paint</CanvasButton>
<CanvasButton variant="charcoal">Charcoal</CanvasButton>
```

### Interactive Effects

```bash
<CanvasButton effect="splash">Click for Splash</CanvasButton>
<CanvasButton effect="brush">Hover for Brush</CanvasButton>
```
